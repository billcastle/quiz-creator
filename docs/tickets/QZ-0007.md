---
id: QZ-0007
title: "Audit skills inventory, update workflow docs, file missing-skills chore"
type: chore
status: done
priority: P1
phase: "02"
assigned: Architect
requested_by: "Phase kick-off"
linked_phase: PHASE-02
pr_urls:
  # - https://github.com/billcastle/quiz-creator/pull/N
---

# QZ-0007 — Audit skills inventory, update workflow docs, file missing-skills chore

## Description

Three deliverables in one ticket:

1. **Skills audit** — list every file in `.claude/skills/` and `.agents/skills/`, verify each is documented in the PHASE-02 skills inventory table (`docs/architecture/PHASE-02-agent-configuration.md`), and identify the four missing skills that need to be created before their respective phases.

2. **Workflow doc review** — confirm that `docs/workflow/dev-workflow.md` and `docs/workflow/qa-workflow.md` contain all QZ-specific conventions required by PHASE-02: branch naming, commit format, PR process, QA trigger, Playwright test naming, and sign-off comment format. Update either file if any required section is missing.

3. **Missing-skills chore ticket** — file a new chore ticket (`QZ-NNNN | chore: create missing skills`) for the four skills not yet in `.claude/skills/` or `.agents/skills/`: `drizzle-orm`, `cloudflare-workers`, `shadcn-ui`, `biome`. The ticket is assigned to Architect, medium priority, and is NOT blocking PHASE-02 completion.

Reference: `docs/architecture/PHASE-02-agent-configuration.md` — Skills Inventory, Skills Still Needed, and Workflow Documents sections.

## Acceptance Criteria

- [ ] Skills inventory confirmed: 7 installed skills documented, 4 missing skills identified
- [ ] `docs/workflow/dev-workflow.md` contains QZ branch naming, commit format, PR process, and pre-commit checklist
- [ ] `docs/workflow/qa-workflow.md` contains QA trigger conditions, Playwright test naming convention, and sign-off comment format
- [ ] Missing-skills chore ticket filed (QZ-NNNN) with all four skill names and the phases that need them
- [ ] Both workflow docs reviewed and confirmed complete by Remy

## Out of Scope

- Actually creating the four missing skills — that is the follow-up chore ticket
- Modifying skill content in existing skill files
- Any application code

## Notes

**Skills audit complete (2026-06-26):**

Installed in `.claude/skills/` (7 total):
- `better-auth-best-practices` — ai-dev (Sage)
- `hono` — ai-dev (Sage)
- `playwright-cli` — ai-qa (Ivy), ai-dev (Nova)
- `react-state-management` — ai-dev (Nova)
- `sqlite-database-expert` — ai-dev (Sage), ai-devops (Axel)
- `tanstack-query-best-practices` — ai-dev (Nova)
- `tanstack-router-best-practices` — ai-dev (Nova)

Note: `.agents/skills/` mirrors all except `hono` (symlinks). All 7 are accounted for.

Missing (4 total) — filed as QZ-0014:
- `biome` — needed by PHASE-03
- `cloudflare-workers` — needed by PHASE-03
- `drizzle-orm` — needed by PHASE-06
- `shadcn-ui` — needed by PHASE-04

**Workflow docs review (2026-06-26):**
- `docs/workflow/dev-workflow.md` — Complete. Contains QZ branch naming, commit format, PR process, pre-commit checklist, ticket lifecycle, and phase completion checklist. ✅
- `docs/workflow/qa-workflow.md` — Complete. Contains QA trigger conditions, Playwright test naming convention (`tests/e2e/<area>/<feature>.spec.ts`), sign-off comment format, and bug report format. ✅

Note: Workflow docs live in `docs/workflow/` (not `.claude/` as the blueprint expected) — this is the canonical location per TAXONOMY.md.
